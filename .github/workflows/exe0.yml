name: exe0: code quality checks

on:
  push:
jobs:

  Testing:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        test: [exe0_cppcheck, exe0_variables]
    steps:
      - uses: actions/checkout@v4
      - uses: insper-embarcados/actions-embedded-check@v1
        with:
          c_file: ${{ matrix.test }}/main.c
